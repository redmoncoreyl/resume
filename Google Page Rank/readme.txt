Compile: g++ main.cpp pageset.cpp wordset.cpp -o webSearch

Run: ./webSearch

After the program is ran, the web needs to be preloaded before
searches can be entered.

Enter single word searches when prompted. The result is a list
of ranked pages that contain the search.
